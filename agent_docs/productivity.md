# Productivity & Tooling

<!--
SETUP NOTE FOR AI: Fill this from the interview — ask what tools the
developer already uses day-to-day (GitHub CLI, database CLIs, project
management integrations) and what would help given their stack, even if
they haven't set it up yet. Only include what's actually relevant to this
specific project/developer — don't pad this with tools nobody here uses.
Delete this note when done, or delete the whole file if nothing applies.
-->

[Tools and workflows that make development faster for this specific
project. Consider, where relevant:

- **Git worktrees** — for working on multiple branches/tasks in parallel
  without stashing or switching context. Worth mentioning if the project
  has long-running feature branches or the developer juggles multiple
  tasks at once.
- **GitHub CLI (`gh`)** — for managing PRs, issues, and reviews from the
  terminal instead of the browser, if the project is on GitHub.
- **Database CLI tools**, if applicable — e.g. the Supabase CLI for local
  dev/migrations if Supabase is in use, or the equivalent for whatever
  database this project uses.
- **Project management integrations** — e.g. Linear, Jira — if the team
  uses one and it's relevant to how work gets tracked.
- **Any other tool the interview surfaces** as part of this developer's
  actual workflow — this list is a starting point, not a ceiling.

Skip anything not actually in use or planned; a tooling list padded with
things nobody touches is noise, not help.]
